version: "3.9"

services:

  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: "cameras"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgreSQL123"

  parser:
    image: parser
    restart: always
    ports:
      - "5432:5432"